// Hide the link so the user doesn't create multiple forms
$("a[data-ingredient=hops]").hide();

// Ensure old invalid form is removed
$("#new_hop_addition").remove();
$(".edit_hop_addition").remove();

$("<%=j render :partial => 'form' %>").appendTo('#hops');
$(".chzn-select").chosen();

